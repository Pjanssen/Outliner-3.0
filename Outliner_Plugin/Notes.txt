GENERAL
-------
* Try making Command unit tests.
* Generalize addNode method in TreeMode.
* Unhide/unfreeze layer when unhiding/unfreezing node
* Superclass sorter does not really work with exceptions (target, bones, etc). -> use imagekey instead?
* SetViewCameraCommand: properly store previous state.
* Check set current layer command. => Old current layer imagekey should be reset somewhere..
* Wirecolor "by layer" button.
* Deselect doesn't work? (in treeview)
* SetActiveLayer rename + description.

TREEVIEW
--------
* Parent highlighting
* TreeNode.PreviousVisibleNode implementation is slow.
* Horizontal resize with flexiblespace does not work well.
* FlexibleSpace.MinWidth
* Add constructor to TreeNode that sets all members, to avoid unnecessary Invalidate calls.

NODE BUTTONS
------------
* Add sort / filter code to click handlers.
* Decide whether clicking a node button should apply to the entire selection 
  with or without holding Control.
* Check if node button click handling can be generalized to get rid of some 
  boilerplate code.

HIERARCHY MODE
--------------


LAYER MODE
----------


SELECTIONSET MODE
-----------------


FLAT OBJECTLIST MODE
--------------------


MATERIAL MODE
-------------

ICON CLICK ACTIONS:
Layer        - Set as current
Material     - 
SelectionSet - 
Bone         -
Camera       - Set active viewport
Geometry     -
Group        - Open/Close?
Helper       - 
Light        - Toggle on/off
Particle     -
Shape        -
Spacewarp    -
Xref         -


IInterface ip = GlobalInterface.Instance.COREInterface;
IGenBoxObject boxObject = ip.CreateInstance(SClass_ID.Geomobject, global.Class_ID.Create((uint) BuiltInClassIDA.BOXOBJ_CLASS_ID, 0)) as IGenBoxObject;
IINode boxNode = ip.CreateObjectNode(boxObject);

Assert.IsNotNull(boxObject);
Assert.IsNotNull(boxNode);
Assert.AreEqual(1, ip.RootNode.NumberOfChildren);

global.COREInterface.DeleteNode(boxNode, false, false);
Assert.AreEqual(0, ip.RootNode.NumberOfChildren);