GENERAL
-------
* Move hidden particle classes 'filtering' to permanent filter?
* Generalize addNode method in TreeMode.
* Labeledit does not receive focus
* Introduce displayname?
* Horizontal scroll
* TreeView base node width
* Refactor TreeMode.tree_SelectionChanged
* Add ?
	// Avoid tooltips popping up when a treenode's text is outside the bounds (gives draw error)
      protected override CreateParams CreateParams
      {
         get
         {
            CreateParams parms = base.CreateParams;
            parms.Style |= 0x80; // Turn on TVS_NOTOOLTIPS 
            return parms;
         }
      }

NODE BUTTONS
------------
* Add sort / filter code to click handlers.
* Decide whether clicking a node button should apply to the entire selection 
  with or without holding Control.
* Check if node button click handling can be generalized to get rid of some 
  boilerplate code.

HIERARCHY MODE
--------------


LAYER MODE
----------


SELECTIONSET MODE
-----------------


FLAT OBJECTLIST MODE
--------------------


MATERIAL MODE
-------------

ICON CLICK ACTIONS:
Layer        - Set as current
Material     - 
SelectionSet - 
Bone         -
Camera       -
Geometry     -
Helper       - 
Light        - Toggle on/off
Particle     -
Shape        -
Spacewarp    -
Xref         -